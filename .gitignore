# Files kept out of version control.

# Reference PDFs are cited in the report, not redistributed here (copyright).
papers/

# Local working/output directory.
output/

# External tool bundles, installed separately: the CalculiX/PrePoMax solver,
# its validation suite, and legacy PrePoMax project archives.
tools/
tests/
archive/

# Editor / tooling state.
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db

# CalculiX / Abaqus binary solver output (regenerated by running the pipeline).
*.frd
*.odb
*.12d
*.sim
*.prt
*.msg
*.env
*.com

# Temporary solver scratch files.
*.tmp
*.sta
*.cvg

# MATLAB autosaves.
*.asv

# LaTeX build artifacts.
*.aux
*.log
*.bbl
*.blg
*.out
*.toc
*.lof
*.lot
*.synctex.gz
*.fls
*.fdb_latexmk

# Not part of this code-only repository (results, figures, and the full
# report are presented in the project report PDF).
report/
results/
docs/
fea/geometry/
project_description.txt
